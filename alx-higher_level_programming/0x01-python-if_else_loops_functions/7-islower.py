#!/usr/bin/python3
# Author - Mario Mokles

def islower(c):
    """the Function to checks  lowercase characters."""
    if ord(c) >= 97 and ord(c) <= 122:
        return True
    else:
        return False
